
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">The Gossip Project</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        <li class="nav-item active">
            <%= link_to "Accueil", static_pages_home_path, :class => "nav-link"%>
        </li>
        <li class="nav-item">
            <%= link_to "Team", static_pages_team_path, :class => "nav-link"%>
        </li>
        <li class="nav-item">    
            <%= link_to "Contact", static_pages_contact_path, :class => "nav-link"%>
        </li>
        </ul>
    </div>
    </nav>
    <div class="card">
      <div class="card-body">
    <h1>T'as gagné petit commère </h1>
    <br>
    <h2>Voici ton potin : </h2>
    <br>
        <% i=params[:id] %>
        <% g = Gossip.find(i) %>
      <p><strong>Titre</strong> <br> <%= g.title %></p>
        <p><strong>Potin </strong><br> <%= g.content %> </p>
        <p><strong>Créé le </strong><br> <%= g.created_at %> </p>
        <p><strong>Auteur </strong> <br> <%= g.user.first_name %> </p> 
        <% user = g.user %>
        <%= link_to "Clique ici pour voir le profil de son auteur", user_id_path(user.id) %>
      </div>
    </div>
   </body>
</html>
